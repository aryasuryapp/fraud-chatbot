# Core dependencies
pandas>=2.0.0
numpy>=1.24.0

# Database
# sqlite3  # Built-in with Python

# Document processing
pypdf>=3.0.0
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1

# Embeddings and vector store
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4  # Use faiss-gpu for GPU support

# LLM providers (choose based on your needs)
openai>=1.0.0
anthropic>=0.18.0
langchain-openai>=0.0.5  # Required for RAGAS
# ollama  # Uncomment if using Ollama

# UI
streamlit>=1.30.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Optional: Kaggle dataset download
kaggle>=1.5.16

# Evaluation with RAGAS
ragas>=0.1.0
datasets>=2.14.0  # Required for RAGAS
